package org.example.google;

import org.example.parserSettings;

public class googleSettings extends parserSettings {
    public googleSettings(int count){
        BASE_URL = "https://www.google.com/";
        this.count = count;
    }
}
